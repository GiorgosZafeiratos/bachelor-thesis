# Requirements

# Thesis: Multimodal Biomedical Signal Classification Using a Hybrid
#         CNN-LSTM Architecture with Adaptive Normalization and XAI

# Install: pip install -r requirements.txt
# Python:  3.9 – 3.11  (3.10 recommended)

# CUDA:    Optional. CPU-only training works but is slow for EEG (64-ch).
#          For GPU:  pip install torch --index-url https://download.pytorch.org/whl/cu118

# Scientific stack
numpy>=1.24,<2.0
scipy>=1.11
pandas>=2.0
scikit-learn>=1.3

# Deep learning
torch>=2.1 # torchvision not needed; torchaudio optional
# For GPU builds, replace the line above with the appropriate wheel URL from
# https://pytorch.org/get-started/locally/

# Biomedical signal loading
wfdb>=4.1      # MIT-BIH & PhysioNet waveform loading
mne>=1.6       # EEG & EDF loading and epoching
neurokit2>=0.2 # ECG R-peak detection (Pan-Tompkins)
# falls back to scipy if unavailable

# Data storage
h5py>=3.9 # HDF5 read/write for processed datasets

# Visualisation
matplotlib>=3.7
seaborn>=0.13

# Progress bars
tqdm>=4.66

# Statistical tests
# scipy.stats (Wilcoxon, Cohen's d) is included in scipy above

# Optional: faster resampling
# resampy>=0.4 # polyphase resampling; scipy.signal.resample_poly
# used by default, resampy is faster for large arrays

# Development & reproducibility
# pytest>=7.4 # uncomment to run unit tests
# black>=23.0 # code formatter
# isort>=5.12 # import sorter
