"""
src — thesis source package

All modules can be imported either:
  from src.baseline_models import CNN1DBaseline # package-style
  from baseline_models import CNN1DBaseline     # flat-style (run from src/)
Both work because main.py inserts src/ into sys.path at startup.
"""
